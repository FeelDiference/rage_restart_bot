# CI/CD пайплайн для автоматического развертывания Rage Restart Bot
# 
# Необходимые секреты в GitHub Repository Settings:
# - SERVER_USER: имя пользователя для SSH подключения к серверу
# - SERVER_SSH_KEY: приватный SSH ключ для подключения к серверу
# - TELEGRAM_BOT_TOKEN: токен Telegram бота (опционально для уведомлений)
# - TELEGRAM_CHAT_ID: ID чата для уведомлений (опционально)
#
name: Deploy Rage Bot

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Переменные окружения
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: rage-restart-bot

jobs:
  # Тестирование кода
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest black flake8
    
    - name: Lint with flake8
      run: |
        # Останавливаем на критических ошибках
        flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics
        # Предупреждения для всех файлов
        flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Format check with black
      run: |
        black --check src/
    
    - name: Run tests
      run: |
        pytest tests/ -v || echo "Тесты не найдены, пропускаем"

  # Сборка и публикация Docker образа
  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ github.repository }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix=commit-
          type=raw,value=latest,enable={{is_default_branch}}
        flavor: |
          latest=auto
    
    # Настройка Docker Buildx для поддержки кэша
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        # Используем buildx для поддержки кэша
        platforms: linux/amd64

  # Развертывание на удаленном сервере
  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout repository for deployment files
      uses: actions/checkout@v4
    
    # Сначала копируем важные файлы
    - name: Copy deployment files
      uses: appleboy/scp-action@v0.1.5
      with:
        host: 78.37.40.131
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 23
        source: "docker-compose.yml,docker-compose.build.yml,config/,src/,Dockerfile,requirements.txt"
        target: "/tmp/rage-bot-deploy/"
        strip_components: 0
    
    - name: Deploy to production server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 78.37.40.131
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 23
        timeout: 300s
        script: |
          echo "🚀 Начало развертывания Rage Bot..."
          
          # Создаем директорию проекта если её нет (без sudo)
          mkdir -p /opt/rage-restart-bot || {
            echo "⚠️ Не удалось создать /opt/rage-restart-bot, используем домашнюю директорию"
            mkdir -p $HOME/rage-restart-bot
            cd $HOME/rage-restart-bot
          }
          
          # Если удалось создать в /opt, переходим туда
          if [ -d "/opt/rage-restart-bot" ]; then
            cd /opt/rage-restart-bot
          fi
          
          # Копируем файлы из временной директории
          echo "📥 Копирование файлов развертывания..."
          if [ -d "/tmp/rage-bot-deploy" ]; then
            echo "🔍 Содержимое /tmp/rage-bot-deploy:"
            find /tmp/rage-bot-deploy -type f -name "*.yml" -o -name "*.txt" -o -name "Dockerfile" | head -10
            
            # Копируем все файлы включая скрытые
            cp -r /tmp/rage-bot-deploy/* . 2>/dev/null || true
            cp -r /tmp/rage-bot-deploy/.* . 2>/dev/null || true
            
            # Если файлы в подпапках, поднимаем их наверх
            if [ -d "rage_restart_bot" ]; then
              echo "📋 Найдена подпапка rage_restart_bot, копируем файлы из неё"
              cp -r rage_restart_bot/* . 2>/dev/null || true
            fi
            
            echo "✅ Файлы скопированы из /tmp/rage-bot-deploy"
          fi
          
          # Если Git репозиторий уже есть, обновляем
          if [ -d ".git" ]; then
            echo "📥 Обновление репозитория..."
            git pull origin main || {
              echo "⚠️ Не удалось обновить репозиторий, используем скопированные файлы"
            }
          else
            echo "📥 Клонирование репозитория..."
            # Используем HTTPS с токеном вместо SSH
            git clone https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git . || {
              echo "❌ Не удалось клонировать репозиторий"
              echo "🔄 Используем скопированные файлы..."
              # Проверяем что основные файлы есть
              if [ ! -f "docker-compose.yml" ]; then
                echo "❌ Критическая ошибка: docker-compose.yml не найден"
                exit 1
              fi
            }
          fi
          
          # Создаем .env файл для production
          echo "🔧 Создание .env файла..."
          DEPLOY_PATH=$(pwd)
          # Конвертируем repository name в lowercase для Docker
          REPO_LOWERCASE=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "GITHUB_REPOSITORY=$REPO_LOWERCASE" > .env
          echo "TZ=Europe/Moscow" >> .env
          echo "COMPOSE_PROJECT_NAME=rage-bot" >> .env
          echo "BOT_MEMORY_LIMIT=256m" >> .env
          echo "BOT_CPU_LIMIT=0.5" >> .env
          echo "LOG_LEVEL=INFO" >> .env
          echo "ENVIRONMENT=production" >> .env
          echo "DEPLOY_PATH=$DEPLOY_PATH" >> .env
          echo "✅ .env файл создан с repository: $REPO_LOWERCASE"
          
          # Останавливаем старый контейнер
          echo "⏹️ Остановка старого контейнера..."
          docker compose down || docker-compose down || echo "Контейнер не был запущен"
          
          # Логинимся в GitHub Container Registry (приватный)
          echo "🔐 Логин в GitHub Container Registry..."
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # ПРИНУДИТЕЛЬНАЯ ЛОКАЛЬНАЯ СБОРКА (временное решение проблемы с docker модулем)
          echo "🔨 Принудительная локальная сборка..."
          
          # Удаляем любые старые образы
          docker rmi ghcr.io/$REPO_LOWERCASE/rage-restart-bot:latest 2>/dev/null || echo "Старый образ не найден"
          docker image prune -f
          
          # Используем compose файл для сборки
          if [ -f "docker-compose.build.yml" ]; then
            echo "📋 Используем docker-compose.build.yml для сборки"
            cp docker-compose.build.yml docker-compose.override.yml
            
            # Проверяем структуру файлов для сборки
            echo "🔍 Проверка файлов для Docker сборки:"
            echo "📁 Текущая директория: $(pwd)"
            ls -la . | head -10
            echo "📁 Проверяем наличие ключевых файлов:"
            [ -f "requirements.txt" ] && echo "✅ requirements.txt найден" || echo "❌ requirements.txt НЕ найден"
            [ -d "src" ] && echo "✅ src/ директория найдена" || echo "❌ src/ директория НЕ найдена"
            [ -d "config" ] && echo "✅ config/ директория найдена" || echo "❌ config/ директория НЕ найдена"
            [ -f "Dockerfile" ] && echo "✅ Dockerfile найден" || echo "❌ Dockerfile НЕ найден"
            
            # Если критичные файлы отсутствуют, скачиваем из GitHub
            if [ ! -f "requirements.txt" ] || [ ! -d "src" ] || [ ! -f "Dockerfile" ]; then
              echo "🔄 Критичные файлы отсутствуют, скачиваем из GitHub..."
              curl -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                   https://api.github.com/repos/${{ github.repository }}/tarball/main \
                   -o repo.tar.gz
              tar -xzf repo.tar.gz --strip-components=1
              rm repo.tar.gz
              echo "✅ Файлы загружены из GitHub API"
            fi
            
            # Принудительная сборка БЕЗ кэша
            echo "⚙️ Сборка образа без кэша..."
            if docker compose build --no-cache rage-bot; then
              echo "✅ Локальная сборка завершена"
              
              # Проверяем установку зависимостей
              echo "🔍 Проверка установленных пакетов..."
              BUILT_IMAGE="ghcr.io/$REPO_LOWERCASE/rage-restart-bot:latest"
              docker run --rm "$BUILT_IMAGE" pip list | grep docker || echo "⚠️ Docker пакет не найден в pip list"
              
              # Проверяем импорт (упрощённая версия для избежания проблем с кавычками)
              echo "🔍 Тестируем импорт docker модуля..."
              if docker run --rm "$BUILT_IMAGE" python -c "import docker, docker.errors"; then
                echo "✅ Docker модуль работает корректно"
              else
                echo "❌ Проблема с импортом docker модуля"
                echo "📋 Список установленных пакетов:"
                docker run --rm "$BUILT_IMAGE" pip list
                echo "🔍 Базовая проверка:"
                docker run --rm "$BUILT_IMAGE" python -c "import docker" && echo "docker OK" || echo "docker FAIL"
              fi
            else
              echo "❌ Ошибка сборки образа"
              exit 1
            fi
          else
            echo "❌ docker-compose.build.yml не найден!"
            exit 1
          fi
          
          # Проверяем что конфигурация существует
          if [ ! -f "config/config.yaml" ]; then
            echo "❌ Файл config/config.yaml не найден в репозитории!"
            echo "📁 Содержимое директории config/:"
            ls -la config/ || echo "Директория config/ не найдена"
            echo "📁 Содержимое текущей директории:"
            ls -la .
            exit 1
          else
            echo "✅ Файл config/config.yaml найден"
          fi
          
          # Создаем необходимые директории
          mkdir -p logs data
          
          # Запускаем обновленный контейнер
          echo "▶️ Запуск нового контейнера..."
          docker compose up -d rage-bot || docker-compose up -d rage-bot
          
          # Проверяем запуск контейнера
          echo "🔍 Проверка запуска контейнера..."
          sleep 10
          
          # Проверяем логи на наличие ошибок
          if docker logs rage-restart-bot 2>&1 | grep -q "ModuleNotFoundError.*docker"; then
            echo "❌ КРИТИЧЕСКАЯ ОШИБКА: Docker модуль всё ещё отсутствует!"
            echo "📋 Полные логи контейнера:"
            docker logs rage-restart-bot
            echo ""
            echo "🔧 Попытка экстренного исправления..."
            
            # Останавливаем контейнер для исправления
            docker stop rage-restart-bot
            
            # Создаем исправленный образ
            echo "🛠️ Создание исправленного образа..."
            docker run -d --name temp-fix "$BUILT_IMAGE" tail -f /dev/null
            docker exec temp-fix pip install --no-cache-dir docker==6.1.3
            docker commit temp-fix rage-restart-bot-fixed:latest
            docker rm -f temp-fix
            
            # Обновляем compose файл для использования исправленного образа
            sed -i "s|ghcr.io/$REPO_LOWERCASE/rage-restart-bot:latest|rage-restart-bot-fixed:latest|g" docker-compose.override.yml
            
            echo "🔄 Запуск исправленного контейнера..."
            docker compose up -d rage-bot
            sleep 10
          else
            echo "✅ Контейнер запускается без ошибок модулей"
          fi
          
          # Ждем запуска и проверяем статус
          echo "⏳ Ожидание запуска..."
          sleep 15
          
          echo "📊 Проверка статуса:"
          docker compose ps || docker-compose ps
          
          echo "📋 Последние логи:"
          docker compose logs --tail=20 rage-bot || docker-compose logs --tail=20 rage-bot
          
          # Проверяем здоровье контейнера
          if docker compose ps rage-bot | grep -q "Up" || docker-compose ps rage-bot | grep -q "Up"; then
            echo "✅ Контейнер успешно запущен!"
          else
            echo "❌ Ошибка запуска контейнера!"
            docker compose logs rage-bot || docker-compose logs rage-bot
            exit 1
          fi
          
          # Очищаем старые образы
          echo "🧹 Очистка старых образов..."
          docker image prune -f
          
          # Очищаем временные файлы
          echo "🧹 Очистка временных файлов..."
          rm -rf /tmp/rage-bot-deploy
          
          echo "🎉 Развертывание завершено успешно!"
    


  # Уведомление о результате развертывания
  notify:
    needs: [test, build-and-push, deploy]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Notify on success
      if: ${{ needs.deploy.result == 'success' }}
      run: |
        echo "✅ Rage Bot успешно развернут!"
        # Здесь можно добавить отправку уведомления в Telegram
        # curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
        #      -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
        #      -d "text=✅ Rage Bot успешно развернут! Commit: ${{ github.sha }}"
    
    - name: Notify on failure
      if: ${{ needs.deploy.result == 'failure' || needs.build-and-push.result == 'failure' || needs.test.result == 'failure' }}
      run: |
        echo "❌ Ошибка при развертывании Rage Bot!"
        # curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
        #      -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
        #      -d "text=❌ Ошибка при развертывании Rage Bot! Проверьте логи GitHub Actions."